import { VideojuegoPipe } from './videojuego.pipe';

describe('VideojuegoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VideojuegoPipe();
    expect(pipe).toBeTruthy();
  });
});
